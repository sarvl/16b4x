;this test checks whether communication with catalyst is possible

	; check if catalyst works 
	xwr 	UI, 0x01
	mov 	R0, 0x01
	pwr 	0x00, R0
	prd 	R0, 0x00

	;if version is anything other than zero, sets the r0
	cmp 	R0, 0x00
	sne 	R0
	mwr 	0x00, R0

	hlt
